package com.rss.coursecatalogservice;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class CourseCatalogServiceApplication {

	public static void main(String[] args) {
		SpringApplication.run(CourseCatalogServiceApplication.class, args);
	}

	// Add an API to course-catalog-service at /catalog/{userId} that returns a hard-coded list of movie + rating information

	//step-1 is creating a controller, REST resource
}
